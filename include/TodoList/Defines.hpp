#pragma once

namespace TodoList::Utility {

enum class Views {
    MAIN_FRAME = 0,
    SETTINGS_FRAME,
    LOGIN_FRAME
};

enum class DEFAULT_PROJECTS {
    TODAY = 0,
    INBOX,
    SIZE,
};

} // namespace TodoList::Utility
